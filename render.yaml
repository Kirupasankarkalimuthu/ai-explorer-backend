services:
 - type: web
   name: ai-explorer-backend
   env: python
   startCommand: playwright install && uvicorn main:app --host 0.0.0.0 --port $PORT
   envVars:
     - key: OPENAI_API_KEY
       sync: false